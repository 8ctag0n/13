[package]
name = "defi_starknet"
version = "0.1.0"
edition = "2024_07"

[dependencies]
starknet = ">=2.6.0"
chain_starknet = { path = "../../../chain/starknet" }

[cairo]
sierra-replace-ids = true

[[target.starknet-contract]]
allowed-libfuncs-list.name = "experimental"
